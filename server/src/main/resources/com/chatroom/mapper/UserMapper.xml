<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatroom.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.chatroom.entity.User">
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="avatar_id" property="avatarId"/>
        <result column="face_id" property="faceId"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id,
        username,
        `password`,
        avatar_id,
        face_id
    </sql>

    <insert id="add" useGeneratedKeys="true" keyColumn="user_id" keyProperty="userId"
            parameterType="com.chatroom.entity.User">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="null != username and '' != username">
                username,
            </if>
            <if test="null != password and '' != password">
                `password`,
            </if>
            <if test="null != avatarId and '' != avatarId">
                avatar_id,
            </if>
            <if test="null != faceId and '' != faceId">
                face_id
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="null != username and '' != username">
                #{username},
            </if>
            <if test="null != password and '' != password">
                #{password},
            </if>
            <if test="null != avatarId and '' != avatarId">
                #{avatarId}
            </if>
            <if test="null != faceId and '' != faceId">
                #{faceId}
            </if>
        </trim>
    </insert>

    <delete id="deleteById">
        DELETE
        FROM user
        WHERE user_id = #{userId}
    </delete>

    <update id="update" parameterType="com.chatroom.entity.User">
        UPDATE user
        <set>
            username = #{username},
            `password` = #{password},
            avatar_id = #{avatarId},
            face_id = #{faceId}
        </set>
        WHERE user_id = #{userId}
    </update>

    <select id="getByUserId" resultType="com.chatroom.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="getByUsername" resultType="com.chatroom.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
    </select>

    <select id="getAll" resultType="com.chatroom.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
    </select>

</mapper>