<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatroom.server.mapper.GroupUserRelationMapper">

    <resultMap id="BaseResultMap" type="com.chatroom.common.entity.GroupUserRelation">
        <result column="id" property="id"/>
        <result column="group_id" property="groupId"/>
        <result column="user_id" property="userId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,
        group_id,
        user_id
    </sql>

    <insert id="add" useGeneratedKeys="true" keyColumn="id" keyProperty="id"
            parameterType="com.chatroom.common.entity.GroupUserRelation">
        INSERT INTO group_user_relation
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="null != groupId and '' != groupId">
                group_id,
            </if>
            <if test="null != userId and '' != userId">
                user_id
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="null != groupId and '' != groupId">
                #{groupId},
            </if>
            <if test="null != userId and '' != userId">
                #{userId}
            </if>
        </trim>
    </insert>

    <delete id="delete">
        DELETE
        FROM group_user_relation
        WHERE group_id = #{groupId}
          and user_id = #{userId}
    </delete>

    <select id="getByGroupId" resultType="java.lang.Integer">
        SELECT user_id
        from group_user_relation
        where group_id = #{groupId}
    </select>

    <select id="getByUserId" resultType="java.lang.Integer">
        SELECT group_id
        from group_user_relation
        where user_id = #{userId}
    </select>

</mapper>